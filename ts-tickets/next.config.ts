import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
    images: {
        domains: ["media.pathe.fr", "via.placeholder.com","placehold.co"], // Add your external domains here
    },
};

export default nextConfig;
